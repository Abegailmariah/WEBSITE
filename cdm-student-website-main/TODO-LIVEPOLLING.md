# Live Polling + Cleanup TODO

## Tasks
1. [x] Remove the duplicate `CdM-Announcement-System-feature-branches` folder
2. [x] Add live polling (refetchInterval) to announcements on Home page (`src/routes/index.tsx`)
3. [x] Add live polling to announcements list page (`src/routes/announcements.tsx`)
4. [x] Add live polling to student dashboard announcements (`src/routes/student.tsx`)
5. [x] Update `.gitignore` to exclude generated artifacts (DB, logs, build txt files)
6. [x] Verify build/type-check — `tsc --noEmit` passed with 0 errors; `npm run build` produced `.output/`

