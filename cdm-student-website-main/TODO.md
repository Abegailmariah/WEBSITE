# TODO

- [x] Investigate the code to determine the likely issue causing “submit concern” to not actually work.
- [x] Implement real submission in `src/routes/submit-concern.tsx` by POSTing form data to the backend endpoint.


- [x] Add basic loading/error handling so the UI doesn’t always claim success.

- [ ] (If needed) Add env/config for the backend URL (or use a sensible default).
- [ ] Verify the flow by running the dev server/build and checking browser/network behavior.


