# TODO — Student Dashboard Updates

## Task
1. Remove the duplicate "Submit a Concern" form from the Student Dashboard (keep only the Submit Concern tab).
2. Add a "My Profile" card in the dashboard.
3. Add search & filter for concerns and announcements.

## Steps
- [x] Remove `submitStudentConcern` import from `src/routes/student.tsx`
- [x] Add search/filter state hooks to `StudentPage`
- [x] Remove the `<SubmitConcernForm />` render from the dashboard
- [x] Add a "My Profile" card section
- [x] Add search & status filter to the "My Concerns" section
- [x] Add search to the "Announcements" section
- [x] Delete the `SubmitConcernForm` function definition
- [x] Verify build/type-check
